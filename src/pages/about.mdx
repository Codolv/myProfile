---
layout: '../layouts/BaseLayout.astro'
title: 'Über mich & Skills – Waldemar Artes'
description: 'Hintergrund, Skills und Zertifizierungen als Senior Java-Backend-Entwickler und Software-Architekt.'
---

import { personalData } from '../data/personalData.js';

# Über mich

{personalData.personalInfo.description}

<div className="mt-8 space-y-6">
  <section className="rounded-xl border border-slate-800 bg-[#0e1102]/60 p-5">
    <h2 className="text-sm font-semibold tracking-tight text-[#faf9f6] sm:text-base">Skills</h2>
    <div className="mt-4 grid gap-4 sm:grid-cols-3">
      <div className="space-y-1">
        <h3 className="text-xs font-semibold uppercase tracking-wide text-amber-100/70">Sprachen & Laufzeiten</h3>
        <ul className="mt-1 space-y-1 text-xs text-amber-50/90 sm:text-sm">
          {personalData.skills.languages.map((skill) => (
            <li className="flex justify-between">
              <span>{skill.name}</span>
              <span className="font-mono text-amber-50">{skill.level}/5</span>
            </li>
          ))}
        </ul>
      </div>
      <div className="space-y-1">
        <h3 className="text-xs font-semibold uppercase tracking-wide text-amber-100/70">Frameworks & Libraries</h3>
        <ul className="mt-1 space-y-1 text-xs text-amber-50/90 sm:text-sm">
          {personalData.skills.frameworks.map((skill) => (
            <li className="flex justify-between">
              <span>{skill.name}</span>
              <span className="font-mono text-amber-50">{skill.level}/5</span>
            </li>
          ))}
        </ul>
      </div>
      <div className="space-y-1">
        <h3 className="text-xs font-semibold uppercase tracking-wide text-amber-100/70">Tools & Infrastruktur</h3>
        <ul className="mt-1 space-y-1 text-xs text-amber-50/90 sm:text-sm">
          {personalData.skills.tools.map((skill) => (
            <li className="flex justify-between">
              <span>{skill.name}</span>
              <span className="font-mono text-amber-50">{skill.level}/5</span>
            </li>
          ))}
        </ul>
      </div>
    </div>
  </section>

  <section className="rounded-xl border border-slate-800 bg-[#0e1102]/60 p-5">
    <h2 className="text-sm font-semibold tracking-tight text-[#faf9f6] sm:text-base">Methoden & Standards</h2>
    <ul className="mt-3 space-y-2 text-xs text-amber-50/90 sm:text-sm">
      {personalData.skills.methods.map((method) => (
        <li>{method}</li>
      ))}
    </ul>
  </section>

  <section className="rounded-xl border border-slate-800 bg-[#0e1102]/60 p-5">
    <h2 className="text-sm font-semibold tracking-tight text-[#faf9f6] sm:text-base">Zertifizierungen</h2>
    <div className="mt-3 space-y-3 text-xs text-amber-50/90 sm:text-sm">
      {personalData.certifications.map((cert) => (
        <div>
          <p className="font-medium text-amber-50">{cert.date}</p>
          <p>{cert.title}</p>
        </div>
      ))}
    </div>
  </section>

  <section className="rounded-xl border border-slate-800 bg-[#0e1102]/60 p-5">
    <h2 className="text-sm font-semibold tracking-tight text-[#faf9f6] sm:text-base">Ausbildung</h2>
    <div className="mt-3 space-y-3 text-xs text-amber-50/90 sm:text-sm">
      {personalData.education.map((edu) => (
        <div>
          <p className="font-medium text-amber-50">{edu.period}</p>
          <p>{edu.institution}</p>
          {edu.details && <p className="text-amber-100/70">{edu.details}</p>}
        </div>
      ))}
    </div>
  </section>

  <section className="rounded-xl border border-slate-800 bg-[#0e1102]/60 p-5">
    <h2 className="text-sm font-semibold tracking-tight text-[#faf9f6] sm:text-base">Wie ich arbeite</h2>
    <ul className="mt-3 space-y-2 text-xs text-amber-50/90 sm:text-sm">
      {personalData.workPhilosophy.map((philosophy) => (
        <li>{philosophy}</li>
      ))}
    </ul>
  </section>
</div>