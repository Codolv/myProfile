---
layout: '../layouts/BaseLayout.astro'
title: 'Projektverlauf – Waldemar Artes'
description: 'Ausgewählte Projekte der letzten Jahre als Senior-Entwickler und Architekt.'
---

import { personalData } from '../data/personalData.js';

# Projektverlauf

Überblick über meine wichtigsten Projekte als Java-Backend-Entwickler und Software-Architekt.

<div className="mt-8 space-y-6">
  {personalData.experience.map((job) => (
    <section className="rounded-xl border border-slate-800 bg-[#0e1102]/60 p-5">
      <h2 className="text-sm font-semibold tracking-tight text-[#faf9f6] sm:text-base">{job.period}</h2>
      <article className="mt-4 border-l border-amber-600/50 pl-4">
        <h3 className="text-sm font-medium text-[#faf9f6] sm:text-base">
          {job.company} <span className="text-amber-100/70">– {job.position}</span>
        </h3>
        <p className="text-xs leading-relaxed text-amber-50/90 sm:text-sm">
          {job.description}
        </p>
        {job.technologies.length > 0 && (
          <p className="mt-2 text-xs text-amber-100/70">
            <strong>Technologien:</strong> {job.technologies.join(', ')}
          </p>
        )}
      </article>
    </section>
  ))}
</div>